import React from 'react';
import {HomeContainer} from "./index";

const MainContainer = () => {
    return (
        <>
          <HomeContainer/>
        </>
    );
};

export default MainContainer;