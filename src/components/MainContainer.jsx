import React from 'react';
import {CreateContainer, HomeContainer} from "./index";

const MainContainer = () => {
    return (
        <>
          <HomeContainer/>
          <CreateContainer/>
        </>
    );
};

export default MainContainer;