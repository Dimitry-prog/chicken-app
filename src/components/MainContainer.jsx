import React from 'react';
import {CreateContainer, HomeContainer} from "./index";

const MainContainer = () => {
    return (
        <>
          <HomeContainer/>
        </>
    );
};

export default MainContainer;